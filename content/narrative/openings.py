# TODO: Sprint B — Opening Type Classification
#
# Opening types: question, scene, assertion, quote, image, memory
# Used for analytics (which openings drive read rates?), not for
# generation constraints. Writer is NOT forced to pick a type —
# the type is classified after writing for data collection.
